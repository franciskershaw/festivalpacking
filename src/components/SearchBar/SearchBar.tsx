'use client';

const SearchBar = () => {
	return (
		<input
			className="border border-gray-300 rounded-md w-full pl-2 py-1 focus:border-gray-500 focus:outline-none"
			type="text"
		/>
	);
};

export default SearchBar;
